files:
  "/etc/nginx/conf.d/00-static-site.conf":
    content: |
      server {
        listen 80;
        location / {
          root /var/app/current;
          index index.html;
          try_files $uri $uri/ =404;
        }
      }
